<div id="articles">
		{{#if articles}}
			{{#each articles}}
				<div class="panel panel-primary">
				  <div class="panel-heading">
				  	{{!-- using handlebars html escaping -> {{{content}}} --}}
				    <h2 class="panel-title">{{{this.headline}}}</h2>
				  </div>
				  <div class="panel-body">
				  	<div class="col-xs-8">
				  		<p class="summary">{{{this.summary}}}</p>
					    <p class="byline">{{{this.byline}}}</p>
					    <p class="link"><a href="{{this.link}}">Follow story here</a></p>
				  	</div>
				  	<div class="col-xs-2 col-xs-offset-2">
				  		{{#unless this.saved_flag}}
				  		<button data-id="{{this._id}}" class="btn btn-success saveArticle">Add to Saved</button>
				  		{{/unless}}
				  		{{#if this.saved_flag}}
				  		<span><i style='color: green;' class='fa fa-3x fa-check' aria-hidden='true'></i>Saved Article</span>
				  		{{/if}}
				  	</div>
				  </div>
				</div>
			{{/each}}
		{{/if}}
		{{#unless articles}}
			{{#if saved}}
				{{#each saved}}
					<div class="panel panel-primary">
					  <div class="panel-heading">
					  	{{!-- using handlebars html escaping -> {{{content}}} --}}
					    <h2 class="panel-title">{{{this.headline}}}</h2>
					  </div>
					  <div class="panel-body">
					  	<div class="col-xs-8">
					  		<p class="summary">{{{this.summary}}}</p>
						    <p class="byline">{{{this.byline}}}</p>
						    <p class="link"><a href="{{this.link}}">Follow story here</a></p>
					  	</div>
					  	<div class="col-sm-2">
					  		<button data-id="{{this._id}}" class="btn btn-default">Add Notes</button>
					  	</div>
					  	<div class="col-md-2">
					  		<button data-id="{{this._id}}" class="btn btn-warning undoSaveArticle">Delete from Saved</button>
					  	</div>
					  </div>
					</div>
				{{/each}}
			{{/if}}
			{{#unless saved}}
			<div class="panel panel-danger">
			  <div class="panel-heading">
			    <h2 class="panel-title">No articles yet</h2>
			  </div>
			  <div class="panel-body">
			    Click on 'Scrape New Articles' to get new articles.
			  </div>
			</div>
			{{/unless}}
		{{/unless}}
	</div>